{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build matching_stats",
            "type": "shell",
            "command": "make bin/matching_stats.x",
            "problemMatcher": [
                "$gcc"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
        {
            "label": "build matching_stats_opt",
            "type": "shell",
            "command": "make bin/matching_stats.opt.x",
            "problemMatcher": [
                "$gcc"
            ],
        }
    ]
}